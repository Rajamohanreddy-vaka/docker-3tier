server {
    listen 80;

    # Serve the frontend files
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Pass API requests to the backend container
    location /api/ {
        proxy_pass http://backend:5000;
    }
}